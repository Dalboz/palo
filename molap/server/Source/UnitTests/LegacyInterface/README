Timings
=======

Timings is program to create and query a cube using libpalo 1.0. Try --help to see the options.

Example
-------

./Timings --create 2 5 20000 3

  Creates cube with 4 dimensions. The dimensions have 10, 10, 10 and 40.000 base
  elements. Each dimensions has two consolidated element which contain half of
  the base elements.

./Timings --setdata 2.97

  Fills the 2.97% of cube with "1".

./Timings --getdata1
./Timings --getdata_multi1

  Query the 16 different aggegrations using either getdata oder getdata_multi.

./Timings --getdata2 <X>
./Timings --getdata_multi2 <X>

  Queries the base cells, taking the first base element in dimension != <X> and
  looping over the base elements in dimension == <X>. The cell values are extracted
  using getdata or getdata_multi.

./Timings --getdata_area1 <X>

  Queries the base cells, taking the first base element in dimension == <X> and
  looping over the base elements in the other dimensions. The cell values are
  extracted using getdata_area.

./Timings --getdata_area2 <X>

  Queries the aggerations, taking the first consolidated element in dimension == <X>
  and looping over the base elements in the other dimensions. The cell values are
  extracted using getdata_area.

Results
-------

./Timings --getdata1

  AA BA CA DA 8.1161
  AB BA CA DA 6.6
  AA BB CA DA 8.4
  AB BB CA DA 6.6
  AA BA CB DA 6.6
  AB BA CB DA 8.4
  AA BB CB DA 6.6
  AB BB CB DA 8.0839
  AA BA CA DB 8.2
  AB BA CA DB 6.6
  AA BB CA DB 8.4
  AB BB CA DB 6.6
  AA BA CB DB 6.6
  AB BA CB DB 8.4
  AA BB CB DB 6.6
  AB BB CB DB 8

./Timings --getdata_multi1

  got 16 values, sum 118.8

./Timings --getdata2 0
./Timings --getdata_multi2 0

  got 10 values, sum 1

./Timings --getdata2 3
./Timings --getdata_multi2 3

  got 40000 values, sum 3161

./Timings --getdata_area1 0

  got 4000000 values, sum 119161

./Timings --getdata_area1 3

  got 1000 values, sum 30

./Timings --getdata_area2 0

  got 4000000 values, sum 59516.1

./Timings --getdata_area2 3

  got 1000 values, sum 59400

